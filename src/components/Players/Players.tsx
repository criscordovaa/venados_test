import React from 'react';
import {Container} from "@material-ui/core";

const Players = () => {
    return (
        <Container>
            Players Page
        </Container>
    );
};

export default Players;
